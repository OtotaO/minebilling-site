{
  "name": "minebilling-site",
  "version": "0.0.0",
  "private": true,
  "description": "Static marketing site and free tools for MineBilling. No build step — this manifest exists only to declare the test dependency, so a clean checkout cannot silently skip the rendered-page tests.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'",
    "test:ci": "REQUIRE_PLAYWRIGHT=1 node --test --test-reporter=tap 'tests/*.test.mjs'",
    "measure": "node tests/measure.mjs 320 360 768"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
